# Include the system's Readline configuration.
$include /etc/inputrc

# Make the up and down arrow keys match on partially typed commands.
"\e[A": history-search-backward
"\e[B": history-search-forward

# Handle Ctrl+Left and Ctrl+Right in urxvt.
"\eOd": backward-word
"\eOc": forward-word

# Handle Ctrl+Left and Ctrl+Right in PuTTY.
"\eOD": backward-word
"\eOC": forward-word

# Don't instruct the terminal to set the 8th bit when a key is pressed with alt
# help down; this is weird, and nobody should be doing it anymore:
# http://invisible-island.net/ncurses/ncurses.faq.html#bash_meta_mode.
set enable-meta-key off
